# Archive Manifest

<!-- Every doc in the repo is accurate or archived — no third state. Rows are
     append-only; restoring something removes its row. Never archive code that
     is imported or executed — only docs, dead scripts, abandoned plans. -->

| Date | What (path in archive/) | Why archived | Restore-relevant? |
|---|---|---|---|
| {{DATE}} | {{ARCHIVED_PATH}} | {{stale — contradicts X / duplicate of Y / dead — unreferenced since Z / abandoned plan}} | {{yes: <when> / no}} |
